---
id: cas-open-questions
title: Open Questions
desc: 'The live register of every unresolved question, who owns it, what it blocks, and how it gets resolved.'
updated: 1785254700000
created: 1785254700000
---

# Open Questions

The project's live register of unknowns. [[state]] says where we *are*; this note says what we
*don't know* and how each unknown gets closed.

**Rules.** Every entry has a stable ID (referenced from commits and other notes), an owner, what it
blocks, and a concrete resolution path — "think about it more" is not a resolution path. Entries are
never deleted; they are marked `resolved` with a link to where the answer lives. New questions get
appended with the next free ID in their section.

**Status:** `open` · `in-progress` · `resolved` · `deferred` (with a reason and a revisit trigger)
**Owner:** `author` = thesis author's decision · `agent` = resolvable by doing the work

---

## A. Measurement unknowns

*Unblocked as of 2026-07-28: Stockfish 18 (AVX2) installed at `C:\stockfish`, 20 cores available.*

| ID | Question | Status | Owner | Blocks | Resolution path |
|---|---|---|---|---|---|
| **A1** | ~~How long does analysing a player's games actually take?~~ **RESOLVED 2026-07-28** → [[experiments.e01-engine-throughput]]. **50 games in 27 s (depth 12), 89 s (depth 15), 403 s (depth 18)** on this machine, zero cash cost. Two surprises: more engine threads made fixed-depth analysis *slower* (1 thread is 2.9× faster per position than 16), so cores go to more games at once, not more threads per game; and wall time is dominated by the longest single game, so production should parallelise **per position**, not per game. **C1 is comfortably satisfied for the deterministic layer.** | resolved | agent | — | — |
| **A2** | ~~Does deeper analysis change the diagnosis, or only the evaluation?~~ **RESOLVED 2026-07-28 — and the answer is inconvenient.** It changes the diagnosis. Against depth 18: depth 15 agrees on 73.6 % of error labels and 83.7 % of blunders; depth 12 on 61.3 % / 69.2 %. **A single move's label is not a stable fact** (L-006). Aggregate recurrence-based claims survive; per-move claims do not. Working setting: depth 15 routine, depth 18 for discussed positions, **depth recorded with every stored signal** since profiles built at different depths are not comparable. | resolved | agent | — | — |
| **A3** | Is a local small LLM good enough for chess explanation and probe dialogue? | **half answered** | agent | C1, the language layer | **The probe half: yes** → [[experiments.e07-reason-classification]]. `llama3.1:8b-instruct-q6_K` classifies a player's stated reason at kappa 0.74 with zero false-ignorance, in 2.66 s, locally, for nothing — and beats an embedding baseline, so it earns its cost. Note this was made easy by design: the model classifies against a *supplied* reason and is never asked a chess question. **The explanation half is untouched**, and it is the harder one, because an explainer generates prose with no supplied answer to check against — exactly where R-03 folklore laundering lives. Resolution path unchanged for that half: one fixed profile, local model against hosted, scored on a rubric. |

## B. Scope & product decisions

*Not derivable from research — these are the thesis author's calls. Recommendations given.*

| ID | Question | Recommendation | Status | Owner | Blocks |
|---|---|---|---|---|---|
| **B1** | Target strength band | **1400–1800** | **RESOLVED 2026-07-28** → [[decisions.0005-scope-band-source-online-only]] | author | — |
| **B2** | Game source | **Lichess API first**, PGN upload as fallback | **RESOLVED 2026-07-28** → same ADR. Note it is a *game* source, not an analysis source — Lichess evals are absent for ordinary amateur games (E1) | author | — |
| **B3** | Over-the-board players in scope? | **No** — online play only | **RESOLVED 2026-07-28** → same ADR; recorded in [[vision]] non-goals | author | — |
| **B4** | Report, conversation, or both? | **Both, with the split settled** — the report is the durable artefact, the conversation is how assessment happens | **RESOLVED 2026-07-28** → [[architecture.interaction]] | agent | — |
| **B5** | Runtime language | **Python** — settled by use; the E01 harness already runs on `python-chess` + Stockfish | effectively settled | agent | — |

## C. Architecture questions for M3

| ID | Question | Status | Owner | Blocks | Resolution path |
|---|---|---|---|---|---|
| **C1** | ~~The structured player profile schema~~ **RESOLVED 2026-07-28** → [[architecture.player-profile]]. Typed Findings carrying evidence, provenance (incl. depth), uncertainty, peer comparison, context and gap-type determination; persistent across sessions. Each field is justified by the specific failure it prevents. | resolved | agent | — |
| **C2** | ~~Minimum-sample / confidence policy~~ **RESOLVED 2026-07-28** → [[architecture.confidence]]. Four tiers; counts in **distinct games**, never instances; gate at 10 games with data per section; `focus` requires held-out split replication; peer-band comparison rather than absolute thresholds. Thresholds are provisional and flagged for validation before the first agent ships. | resolved | agent | — |
| **C3** | ~~Orchestration pattern~~ **RESOLVED 2026-07-28** → [[decisions.0006-staged-blackboard-orchestration]]. Staged blackboard; section agents parallel and isolated; **no inter-agent messaging**. Conversational swarms rejected on cost, determinism, ablatability and explainability — and because agents measuring different things have nothing to argue about. | resolved | agent | — |
| **C4** | ~~Precompute-once vs per-player split~~ **RESOLVED 2026-07-28.** Per-player engine analysis is cheap enough (E01) that little needs precomputing; what is precomputed once is the **corpora** (puzzle themes, openings/traps, peer reference rates) and the shared position cache keyed by (position, engine, depth) → [[decisions.0007-storage-sqlite-cache-json-profile]]. | resolved | agent | — |
| **C5** | ~~Is [[decisions.0002-compute-first-speak-last]] accepted or rejected?~~ **RESOLVED 2026-07-28 — accepted.** Independent convergence with shipped prior art, plus E01 showing the deterministic layer costs ~89 s per player at zero cash. | resolved | agent | — | — |

## D. Domain knowledge gaps

| ID | Question | Status | Owner | Blocks | Resolution path |
|---|---|---|---|---|---|
| **D1** | ~~The deliberate-practice variance figure is cited from memory~~ **RESOLVED 2026-07-28.** Attributed: Hambrick, Oswald, Altmann, Meinz, Gobet & Campitelli (2014), *Intelligence* 45, 34–45 — deliberate practice accounts for **about one third of the reliable variance** in chess. Residual task: check the exact phrasing against the PDF before quoting (*reliable* variance ≠ total variance). Consequence for the vision: the swarm must never promise rating gains it cannot evidence. | resolved | agent | — | — |
| **D2** | ~~Mapping Lichess puzzle themes → the K2 motif list~~ **RESOLVED 2026-07-28** → [[domain.puzzle-themes]]. 75 themes taken from Lichess's own source file rather than the 250 MB dump. Mapped onto K2 in both directions. Two findings: (a) `quietMove` and `defensiveMove` are *calculation* skills with no slot in our concept map — a player who solves forcing puzzles but fails quiet ones has a specific nameable weakness, so **K3 gains a section**; (b) **not one positional tag exists** in the entire vocabulary, confirming the tactics/strategy asymmetry from a third independent direction. | resolved | agent | M2 | — |
| **D3** | Typical-plan catalogues per pawn structure | open | agent | any planning agent | Needs a primary source (classic middlegame material). Pairs with F2. |
| **D4** | ~~Can we build board-feature detectors for the positional taxonomy?~~ **RESOLVED 2026-07-28** → [[experiments.e02-positional-detectors]]. **Yes — 16/16 hand-verified, 15/15 unit tests, a few hundred lines, no engine or model.** The feared blocker does not exist. **But the risk relocated rather than vanished:** isolated pawns appear in 74.7 % of positions and 96 % of games, so presence carries almost no diagnostic information, and several *correct* outposts were edge knights no coach would mention. New question **C6** below inherits it. | resolved | agent | — |
| **C6** | ~~How is a detected feature weighted for relevance?~~ **RESOLVED 2026-07-29 — by peer deviation** → [[architecture.peer-reference]]. Route 1 (error co-occurrence) was tested and largely negative in E03; route 3 (peer-population deviation) is now built and demonstrated: it removed two of four apparent weaknesses and halved the magnitude of the two that survived (L-012). Route 2 (recurrence) remains available and unused. Caveat carried forward: the first reference has only seven players and is a demonstration of the mechanism, not a trustworthy population. | resolved | agent | — |
| ~~C6 (original entry)~~ | *(superseded — kept for the trail)* | resolved | agent | Tier 2 sections | **Route 1 (error co-occurrence) tested and largely negative.** Pooled lifts 0.87–1.26; drift no better; game phase predicts errors better than any feature; the one striking phase-stratified effect **failed to replicate on held-out players and reversed sign** (L-008). Only `own_outpost` held up across both samples and both measures — a lead, not a result. **Remaining routes:** peer-population deviation (asks whether the player is *unusual* rather than whether the feature predicts errors, so E03's result does not touch it — and the same free corpus doubles as evaluation metric D2); recurrence (C2 — measures consistency, not importance); and a new one E03 suggests, outcome-level association (does the player *score* worse in such games?). |
| **C7** | **Operational definitions for the intent-level concepts** — prophylaxis, restraint, harmony, activity *(new)* | open | agent | Tier 3 sections | → [[domain.hard-concepts]]. Activity reduces to safe-mobility metrics; restraint to prevented pawn breaks; prophylaxis to an engine-assisted test (a quiet move after which the opponent's previously-best continuation has lost its value); harmony to a composite index rather than a detector. Sub-task: **check whether Stockfish 18's `eval` still exposes decomposed evaluation terms** — if so it is the cheapest concept source available. Deliberately scheduled *after* C6. |
| ~~D4 (original entry)~~ | *(superseded — kept for the trail)* | resolved | agent | all positional agents | Still the biggest technical risk, but better posed. The original framing assumed no positional taxonomy existed; that was wrong — *My System* is a positional concept catalogue and Silman's imbalances are another. What is missing is **machine labels**, not vocabulary. So this becomes the same shape of problem as tactical motif detection, which prior art shows is tractable (L-004). Resolve by experiment: take 3–4 concepts from the free corpus (knight outpost, isolated queen's pawn, backward pawn, open file control), implement conservative detectors over `python-chess`, and check them against real games by hand. Report precision honestly. |
| **D5** | How long do coaching interventions take to show results? | open | agent | V6 time estimates | Weakest-evidenced part of the vision. Likely honest answer: give ranges, then *measure* whether the predicted progress sign appeared (V7 doing the work the literature cannot). |
| **D6** | Adult improvers vs. juniors — different sequencing and expectations? | open | agent | B1, M2 | Tier 2/3 reading in F2. |
| **D7** | Is there published methodology for eliciting knowledge in dialogue? | open | agent | V9 probe design | Search *educational assessment* literature, not chess literature — this is a general pedagogy problem. |
| **D8** | **Can a coached player actually meet the target?** — the *power* of the progress check | **partly answered** | agent | V7, and any claim that coaching works | → [[experiments.e06-progress-power]], the proxy-cohort route. **The target is reachable** — 4 of 16 improver predictions met it, so the catastrophic case (a target nothing can reach) is ruled out. Improvers met at **25 % against 8 %**, and the regression confound is measurably absent (rating gain vs early extremity r = +0.059). **But p = 0.120 on 7 met events** — suggestive, not established, and R-13 applies. Remains open for the claim *"the swarm's targets detect improvement"*, which needs a treated cohort (outside C1) or considerably more data. Distinct from D5: that asks *how long*, this asks *whether at all*. |
| **D12** | ~~**The confidence policy tests significance, not magnitude**~~ **RESOLVED 2026-08-05.** `focus` now requires `rate >= baseline * FOCUS_MARGIN` (1.25×) as well as the interval excluding the baseline — magnitude *and* significance. Applied to the **point estimate**, where `PRIORITY_MARGIN` applies to the interval's lower bound, so the tiers keep their ordering: `focus` means "big enough and reliably real", `priority` means "big enough even at the pessimistic end". **The floor was chosen from the data, not picked:** every one of the 40 findings the swarm currently asserts on real players was measured, and the **smallest ratio is 1.40**, so 1.25 removes none of them. Re-measured end to end afterwards — 19 players advised, 13 claim kinds, overlap 0.08, 29/29 grounded, all unchanged. The rule exists to stop the *next* large-denominator section, not to prune the present ones. S5 keeps a stricter local bar for a different reason: E03 means its claims cannot say what they cost. | resolved | agent | — | — |
| **D14** | **`NO_CHANGE_RATIO` is fitted for an error floor the analysis no longer uses** *(new 2026-08-16)* | **open — and flagged in the output** | agent | V7, and every "met / not met" verdict | [[experiments.e33-error-threshold]] lowered `INACCURACY_WP` 10.0 → 5.0. `NO_CHANGE_RATIO = 0.58` was fitted at 10.0 from 57 predictions across 84 players, cross-validated at 2 and 5 folds ([[experiments.e05-natural-drift]]), and a different floor means different rates, different regression, different constant. **Not hidden:** `labels.NO_CHANGE_RATIO_FITTED_AT_WP` records the floor it was fitted at, `calibration_is_stale()` compares it with the floor in use, and while they disagree the plan withdraws the "15–23 % reach this anyway" figure and states why. The target itself still stands — only the false-positive rate is unquotable. **Resolution path:** re-fetch an ~84-player corpus with `fetch-corpus` (which now exists, and did not when E05 was run), re-run `e05-natural-drift/run.py` at the new floor, re-fit with `calibrate.py`, then update the marker. Roughly four hours of engine time and no new thinking. Blocking nothing except the progress check's headline number |
| **D13** | ~~**The swarm names what punished a player and never why they allowed it**~~ **RESOLVED 2026-08-17, for 2 of 4 candidates** → [[experiments.e34-material-causes]]. `chesscoach/material.py` implements the reviewer's own list over **the move the player actually played** — the first detectors in the project to read it, closing the structural gap E33 named — at 1.5 ms per move and no engine call. All four separate players (spreads 1.36–1.75×, alongside `fork` at 1.40×). **Two are refused anyway**, by the test that closed S7: `left_hanging` correlates **+0.917** with the player's overall error rate and `ignored_threat` **+0.914**, against the +0.737 that killed `missed_quiet` — a player who errs more has more loose pieces as a *consequence*, so those rates are error-proneness renamed (L-014). The survivors `moved_into_attack` (+0.225) and `miscounted_exchange` (+0.123) ship as **S7 · material safety**, reopening the slot E10 emptied. Anonymous errors fall 52 % → 49 %; for the 488 errors they explain, the report can finally say what the player *did*. **Still open underneath:** the gap between "did not check the square" and "checked and misjudged it" is invisible in a game record and needs a probe (D10), and the two refusals may survive a formulation that is not a near-restatement of erring. | **resolved in part** | agent | — |
| **D16** | ~~**`seconds_spent` understates thinking time by the increment**~~ **RESOLVED 2026-08-20** → [[experiments.e45-increment-correction]]. `increment_seconds()` reads the tag, `Observation.increment` carries it, `seconds_spent` adds it back; 8 tests. `instant_move_error` falls **22.40 → 17.69** wp/game in rapid (−21 %) and 34.33 → 32.16 in blitz, **still leading the band note in both strata** — the number was wrong, the claim was not. `long_think_error`'s rate *rose*, since correcting a clock moves things both ways, and `time_pressure_error` did not move, correctly. Control: **0 clock-free cells changed**. One leading finding changed across the twelve review players (Sheriwoyama). | resolved | agent | S2's three claims, and E25's band note | → [[experiments.e44-clock-on-noted-moves]]. `Observation.seconds_spent` is `clock_before - clock_after`; with an increment `i` the clock reads `before - spent + i`, so the stored value is `spent - i`. On a 180+2 game a 3.5 s move records as **1.5 s** and trips `INSTANT_MOVE_SECONDS = 2.0`. Only `chesscoach/evaluation/planted.py` subtracts an increment — the analysis path never does. **Not affecting E44**, whose games are 91 % increment-free, and all 89 flagged moves survive the correction. **But `instant_move_error` is the most expensive claim this project has measured** (16.1 wp/game, [[experiments.e25-shared-weaknesses]]), and it is precisely the claim this inflates, on precisely the games where increments are common. **Resolution path:** add the increment to `seconds_spent`, then re-measure what share of the peer corpus is increment-bearing and whether the band note's figure moves. Cheap — the clock is already parsed and no engine time is needed. |
| **D22** | **A claim that names a circumstance is not actionable; one that names a behaviour is** *(new 2026-08-23)* | **open — design done** | agent+author | V4, V6, and every claim's wording | → [[design.informative-claims]]. The author marked 23 examples and the pattern generalises past the two claims that prompted it: *"you go wrong early"* and *"you go wrong when winning"* are **circumstances** — true, detected correctly, and impossible to act on. *"you leave the Scandinavian at move 7"* and *"when ahead you stop defending the king"* are **behaviours**. `advantage_error` scored **0 ✓ / 0 ✗ / 5 ?** — the author was not unsure whether the moves were errors, but what the claim was telling them, and their own notes name the content (*"didn't defend king properly"* ×2, *"deformed pawn structure"* ×2). `early_error` scored **0 ✓ / 4 ✗**, all rejecting the *explanation* rather than the detection. **Several shipping claims would fail this test** and deserve their own pass. |
| **D23** | ~~**`moved_into_attack` fires on ordinary exchanges**~~ **RESOLVED 2026-08-23** | **resolved** | agent | V4, S7 | → [[design.informative-claims]] § Problem 1. **0 of 5** on the author's marks, and reproduced from their own counterexamples: all five are captures with `exchange_value >= 0`, including `Bxd8+` at **+6**, which *wins a queen* and is reported as *"you put the piece you have just moved on a square where it can be won."* The guard excludes only *losing* captures, so an even-or-better capture falls through to a test that measures the recapture **in isolation** and ignores what was captured. Every ordinary trade fires. **Fix:** `exchange_value` nets the whole sequence and for a quiet move equals `-swap` on the destination, so one test covers both — `moved_into_attack` = quiet move with `exchange_value < 0`, `miscounted_exchange` = capture with `exchange_value < 0`. Clean partition, restores what the docstring always claimed. **Applied.** All five counterexamples are now tests, replayed from the author's own games. Firing on player moves falls **10.1 % → 1.8 %** — the old detector was firing on one move in ten, which is how completely ordinary trades dominated it. **The peer reference had to be rebuilt**, because it was built with the broken detector: at 1.8 % against a stale 11.2 % baseline every player fell below `baseline_rate` and the claim vanished for all twelve. Rebuilt, the population reads **11.22 % → 1.72 %** rapid and 11.65 % → 1.91 % blitz, while `miscounted_exchange` is **unchanged at 1.92 %/1.81 %** — the control, since it was never touched. The claim returns at **36 instances across 3 players**, and the two now sit at ~1.8 % and ~1.9 %, which is what a clean partition of "loses material" split by capture-versus-quiet should look like. |
| **D17** | **The motif detectors use a naive safety test and never ask whether the tactic wins anything** *(new 2026-08-23)* | **open — partly fixed** | agent | V4, and every section that consumes a motif | → [[decisions.0011-detection-correctness-over-expert-agreement]]. `tactics.py:280` `_lands_safely` is *"attacked by the enemy → is it defended by me?"*: **no piece values, no exchange evaluation**, so a knight landing where a pawn attacks it counts as safe if anything defends it. `_is_fork` then requires only **two attacked targets** — it never asks whether the opponent can defend both, capture the forker, or whether the fork **wins material at all**. Meanwhile `chesscoach/material.py` already implements a static exchange evaluator (`_swap`) that `tactics.py` never calls: **two notions of "safe" in one codebase, and the motifs use the weaker one.** The author's reading also reports `moved_into_attack` firing on ordinary exchanges and king-attack claims raised deep in endgames. **Resolution path:** rebuild the safety test on SEE, add a wins-material requirement, then re-screen every motif on hand-verified samples the way [[experiments.e04-motif-precision]] did when it caught two over-firing detectors. **Precision per detector is the deliverable**, not a pass/fail. **Progress 2026-08-23:** `material.wins_material()` gives one exchange-based answer for either side, and `_lands_safely`, `_is_worth_winning` and `_is_winnable` now use it. A position was found where the old test called a knight safe on a square attacked by a pawn and defended by a pawn, and the detector called the resulting piece-drop a **fork**; that is now refused. **But the change reached only 2 of 9 motifs** — those three helpers are used by `fork` and `trappedPiece` alone. Measured over 8,646 real played moves, firings went **fork +20 %, trappedPiece +31 %, everything else unchanged**, and the *increase* is because the worth-winning test got more permissive (an exchange finds defended-but-winnable targets that "is it undefended" missed) while the safety test got stricter. **Whether that is better cannot be settled by counting** — `results/verification-sheet.txt` samples firings per motif with a position, SAN and a link, for the author to mark. **All nine rebuilt 2026-08-23.** `_is_pin` now requires the pinning piece to survive, verifies an absolute pin with `is_pinned`, and requires a relative one to win the piece behind once the front steps aside; `_is_skewer` likewise. `_is_discovered_attack` requires the revealed target to be winnable. `_is_hanging_piece`/`_is_hanging_pawn` settle "free" with `exchange_value` rather than `is_attacked_by`, which counts a **pinned** defender correctly. `_is_back_rank_mate` requires mate **along the rank** by a rook or queen, so a smothered mate on the eighth is no longer reported as one. `_is_removing_the_defender` requires the capture to be sound and the loosened piece winnable. S8's king-pressure claim now excludes endgames (`attacking_phase`), which is the author's *"king attacks are recorded deeply in the endgames"*. **Firings over 8,646 real played moves: pin 526→332 (−37 %), discoveredAttack 176→94 (−47 %), skewer 50→29 (−42 %), backRankMate 14→5 (−64 %), hangingPawn 454→648 (+43 %), hangingPiece 488→568 (+16 %), capturingDefender 217→302 (+39 %), fork 229→274 (+20 %), trappedPiece 91→119 (+31 %).** The rises are all the same cause: an exchange finds targets that "is it undefended" refused, including defenders that are pinned and cannot actually recapture. **Two old fixtures were unsound and had to be rebuilt** — both skewer positions put the rear piece defending the front one, so `...Qxa1` simply won a rook; they exercised the geometry and were not tactics (L-044 again). **Precision is still unmeasured**, and the instrument for it now covers everything rather than the motifs alone: `detection_sheet.py` writes **one sheet of all 33 claims that fired**, 5 real examples each in one uniform shape — player, move number, colour, the move in SAN, what it cost, and a link that opens the game at that move — with a `[y]/[n]/[?]` box per line. It is built on `Measurement.instances_at` (added for D15), so it can cite real instances of *any* claim rather than only of those with sampled evidence, and it includes the **sub-threshold pool**, since a claim that failed the confidence gate still fired on real moves and is exactly what needs checking. It also lists the **18 claims that never fired at all** in these games, because a detector that is silent everywhere is as much a defect as one that is wrong — among them `missed_motif.hangingPiece`, which is odd beside `missed_motif.hangingPawn` firing normally and is worth a look. |
| **D18** | ~~**Cited evidence is unreadable to a chess player**~~ **RESOLVED 2026-08-23.** Moves render in SAN (`Evidence.played_san`/`better_san`, falling back to UCI on an unreadable FEN or an illegal move rather than taking a report down), and every citation now names the colour, the opponent and the date and carries `lichess.org/<id>#<ply>`, which opens at the move. `Evidence.from_observation` replaced nine hand-built constructions — with an `ENGINE_BEST` sentinel, because S5, S6 and S8 *deliberately* show no alternative and a plain `None` default would have silently deleted that. **And it exposed a real content bug:** S1 stored the opponent's punishing move in `better_move`, so every `allowed_motif` example told the player a move they could not make "was better" — often not legal for their colour. It is now `Evidence.opponent_reply`, rendered in the position it was actually played in, and reads *"you played O-O-O, and Nxe5 punished it"*. | **resolved** | agent | V8 | Moves print as UCI (`g8f6`) where a player reads SAN (`Nf6`), and games are cited by bare id with no colours, opponent or date, so the player cannot find the game in the Lichess app. `Evidence` carries the FEN, so SAN is a local conversion; `GameRecord` already holds white, black and date. Not a design question — but it is what makes every other check in this list faster for the author, so it is worth doing early rather than last. |
| **D19** | **"The edge of your repertoire" is the wrong explanation for early errors at this level** *(new 2026-08-23)* | **open** | agent+author | V4, V6, and S4's wording | The author: *"most players at this level will get out of the opening lines before move 8 or just until the castling"*, so citing examples around move 15 and calling it the edge of a repertoire is unsupported. **Two testable replacements**, both stronger than the current wording: (a) check the played moves against an opening book and locate where the player **left theory**, then ask whether their early errors cluster at that point — that is a definitive statement rather than an inference; (b) if the errors come later, the gap is not the line but the **ideas** — the plans and the weaknesses the opening creates — which is only assertable if it repeats across the *same* opening. Either way the advice should carry that opening's ideas. **Needs an opening book** (free and public: Lichess's own, or a PGN of ECO lines), which is a new data source and therefore a design note before code. |
| **D20** | **The advice is not actionable enough for the level it targets** *(new 2026-08-23)* | **open** | agent+author | V6, and the C7/R-03 pair | The author: it matters less that the advice is what a human would say than that the **detection** is right and that the player gets *"some concrete steps to train and that it is explained with simple wording and not left for player to make their own conclusions."* The tension to resolve first: sourcing training material collides with **C7** (free research materials only) and **R-03** (no folklore laundering — LLM-generated chess advice is not a source). So this is not "search the internet and paste"; it needs sources with an evidence class recorded, per [[capacity.knowledge]]. **Blocked behind D17 on purpose:** actionable advice attached to a wrong detection is worse than vague advice attached to a right one. |
| **D21** | **Does the 5-point error floor produce examples a player cannot understand?** *(new 2026-08-23)* | **open — parked for discussion with the author** | agent+author | V8, and [[experiments.e33-error-threshold]] | The author reports cited examples where *"I couldnt understand at all for what reason that move is that much more better"*, and some that are counterintuitive. **The agent's hypothesis, raised and explicitly deferred rather than acted on:** E33 lowered `INACCURACY_WP` from 10.0 to 5.0, roughly doubling what is detected, and the added half is by construction the *marginal* moves — engine preferences with no material or clear positional consequence. If so this is a threshold decision rather than a detector defect, and the two would need separating before either is changed. **Not being worked on.** Recorded so the question is not lost and so that, if the D17 re-screen finds the detectors sound on the examples the author disputed, this is the next place to look. |
| **D15** | **The reviewer's top concern is excluded by category width, window size and threshold — not by a judgement about importance** *(new 2026-08-20)* | **open** | agent | V4, the arbiter, and every expert-review verdict | → [[experiments.e41-cost-ranking]]. The reviewer proposed ranking by severity and count instead of peer excess. Measured against their own notes it agrees **0 of 6**, with a *worse* median overlap (0.0 against 0.5), so it is **refused as stated**. But two rules disagreeing about nearly every claim and producing the same answer means the ranking rule was never the cause, and dumping the candidate lists found three defects, none of them in the arbiter. **(a) 5 of 12 material claims never become candidates at all** — `bjagus` and `maikel5` have neither, and they are the two players whose notes are most dominated by hanging material ([[experiments.e31-move-level-agreement]], [[experiments.e32-hanging-pawn-screen]]). **(b) The 7 that are measured are above the peer rate on every one**, costing **6.3–15.1** wp/game, and are still blocked: `FOCUS_GAMES_WITH_DATA = 20` against a **20-game window** yields 18–19 games with data (cademan 7/18, goydorak 8/19), and `FOCUS_MARGIN = 1.25` excludes ratios of 1.14× and 1.21×. **(c) In the cost pool they lose to claims that contain them** — `s4_opening_outcomes.py:141` tallies `early_error` for *every* diagnosable error in the opening window, hung pieces included, so a phase bucket outprices a motif by construction. **Cost ranks category width, not importance.** `drop_redundant_aggregates` already exists for this and operates only *within* a section. **Update 2026-08-20** → [[experiments.e42-claim-overlap]]: **(c) is corrected and largely refuted.** Measuring the instance sets shows the claims barely overlap — 501 cross-section pairs, median **3 %** coverage, **none** reaching 80 %. `early_error` outprices `hangingPiece` because more errors happen in the opening than hanging pieces happen anywhere, not because it contains them. The suppressor was built anyway (`chesscoach/overlap.py`, threshold **0.60** calibrated against the pairs `drop_redundant_aggregates` already deletes) and changes **one player of twelve**. **Update 2026-08-20** → [[experiments.e43-focus-gates]]: **(b) is measured and half of it is refuted.** `FOCUS_GAMES_WITH_DATA = 20` blocks **15** claims at a 20-game window and **0** at 60 — a threshold measuring the window, not the evidence — and expressing it as a fraction of the corpus fixes it completely at any fraction from 0.70 to 0.90, taking window agreement with the full corpus from **66 % to 75 %**. Among the fifteen: cademan's `allowed_motif.hangingPiece` at **1.84×**/15.1 wp and bernes' at **2.60×**/10.5 wp — the reviewer's own top concern, silenced by arithmetic about corpus size. **`FOCUS_MARGIN` is exonerated:** lowering 1.25 → 1.15 admits **+0** claims at 20 games and +2 at 60, the two claims E41 named as its casualties fail for a different reason (the interval test), and S5's **pooled** claim does not return even at **1.10**. The margin stays. **Recommended and not applied:** the fraction fix changes nothing at 60 games but does change the reports the author is mid-way through grading, so applying it is their call. **(a) remains untouched.** **Resolution path:** cross-section aggregate suppression is **done and small**; the two focus gates need re-examining against the review window's size; the five absent claims are a detector-threshold question and are unrelated to ranking. **Also note (b) contaminates the expert review itself** — a claim can be blocked purely because the reviewer read twenty games rather than twenty-five. |
| **D11** | **The endgame is largely undiagnosable under the current eligibility rule** *(new 2026-08-05)* | open | agent | S3, and any later endgame work | Building S3 found that **3 of 24 games** for a real player reached an endgame still inside `DECIDED_CP`. Structural, not tuning: `diagnosable()`'s window exists because win-probability labels compress at the extremes (L-009), and the endgame is precisely where games are most often already past it. **A rule designed for the middlegame removes most of the endgame.** Two routes, neither cheap: an error label for endgames that does not depend on win-probability deltas (e.g. distance-to-result, or tablebase-exact for ≤7 pieces — free and public), or accepting that this project can *describe* the endgame and not *diagnose* it, and saying so. Until then S3 speaks through its pooled claim and its advantage-retention claim, both of which work. |
| **D10** | **The prober's ground truth is written and labelled by its author** *(new 2026-08-04)* | **open — but no longer blocking** | agent+author | V2, V9, and any use of the prober on a real player | → [[experiments.e07-reason-classification]]. The classifier question is settled (`llama3.1:8b-instruct-q6_K`, kappa 0.74, zero false-ignorance). What is weak is the *labels*: 46 answers I invented, labelled by me, with the 0.74 measured in-sample because the refusal split came after seeing which items failed. **A better model cannot fix any of that.** Resolution path: ~40 answers collected from people who are not the author — a position, "what would you play and why", free text — and labelled by someone else. Cheap, unglamorous, and nothing downstream can be believed without it. Also the first real test of `declines_to_answer()`, which is a fixed phrase list and will miss what it has not seen. **Decision 2026-08-06: the gate is opened in `cli coach`, and closed by default in the standalone `probe` command.** A prober whose results cannot reach the diagnosis makes V9 demonstrable and inert, and the goal is a complete system. What replaces the gate is **provenance in the output**: the report states that a gap type came from *n* questions read by a classifier that agrees with its own rubric about three times in four, so the reader can weigh it. Honest and complete, rather than honest and disconnected — but the underlying weakness is unchanged and this question stays open. |
| **D9** | ~~**The no-change constant is depth-dependent and the planner applies one value**~~ **RESOLVED 2026-08-03** → [[experiments.e05-natural-drift]] § D9. Same 84 players, outcome period held whole, measurement period capped at 30/45/60/78 games. The dependence is **real, monotone and modest**: the fitted constant runs **0.488 → 0.594**, and with a single 0.58 the share met by drift runs **23 / 22 / 18 / 15 %**. **Two corrections to L-019 fell out:** 0.34 was never a fitted value at any depth (it was a hand-tightened guess from 13 predictions — the fit at that depth is 0.488), and drift at K=30 is +7.4 %, not the +11.2 % attributed to thinness. **Resolution:** no per-depth constant — four points do not determine a curve and fitting one is L-018's overfitting. The output states the measured **range (15–23 %)** instead of a point, so the thin-history penalty is visible to the player rather than hidden. | resolved | agent | — | — |

## E. Facts to verify before they are relied on

| ID | Claim | Status | Owner | Resolution path |
|---|---|---|---|---|
| **E1** | ~~Lichess API parameters~~ **RESOLVED 2026-07-28** by fetching 59 real games (`experiments/e01-engine-throughput/fetch_games.py`). `clocks=true` → `%clk` comments present ✔. `opening=true` → `ECO` + `Opening` headers present ✔. `division=true` → **no effect on PGN output** (it is a JSON-export field, not a PGN one). `evals=true` / `accuracy=true` → **returned nothing**, because *those games were never analysed on Lichess*. **This is the important finding: evaluations are only present for games a user chose to analyse, which for ordinary amateur games is rare. We cannot rely on Lichess-supplied evals and must run our own engine** — which makes A1 load-bearing rather than merely informative. | resolved | agent | — |
| **E2** | "50% of tournament games reach rook endings" — widely repeated, unsourced | open | agent | Verify against the Lichess database or drop it. Pleasingly, this is *computable* — a good early sanity exercise for the analysis pipeline. |
| **E3** | Puzzle database schema and size | in-progress | agent | Download the header and confirm. Pairs with D2. |

## F. Research debts

| ID | Debt | Status | Owner | Blocks | Resolution path |
|---|---|---|---|---|---|
| **F1** | ~~**Five prior-art projects identified but unread.**~~ **RESOLVED 2026-07-28** → [[domain.prior-art]]. All five cloned and read. Headlines: Arrakis Engine independently implements ADR-0002's architecture (so **C5 → accept**); its escalation-tier design is a concrete answer to **C2**; tactical motifs are deterministically detectable in ~900 lines of pure `python-chess` (L-004); error classification must use win probability, not raw centipawns (L-005); **none of the five evaluates whether its coaching is correct**, which makes [[evaluation]] genuinely novel work. Licence constraint found: Arrakis is **AGPL-3.0** (read and reimplement, never copy); Iamsdt/chess has **no licence** (unusable). Original entry follows. — **Five prior-art projects identified but unread.** What is recorded about them comes from *search-result snippets only* — no file has been opened, no licence checked, no architecture seen. Lead list, not knowledge. | open | agent | M3 (properly — designing before reading is the mistake) | Clone all five; read each against a fixed question list: ① licence ② architecture — does anyone else land on compute-first-speak-last? ③ motif detection technique ④ stateless vs. persisted player model ⑤ tokens/calls per game ⑥ do any of them *evaluate* coaching correctness (prediction: none — if so, that is a contribution claim for the thesis) ⑦ issues & README caveats. **Output:** `domain.prior-art`, with "what we adopt / avoid / do differently" — reusable as the thesis related-work section. |
| **F2 (revised)** | **Primary coaching sources unread — now under constraint C7 (free materials only).** The original plan named paid books; that is off the table except to unblock. **Revised free plan, in order:** ① **Capablanca, *Chess Fundamentals*** ([Project Gutenberg #33870](https://www.gutenberg.org/ebooks/33870), public domain in the USA) — graded pedagogy from a world champion, direct evidence for the band-dependent priorities; ② **Nimzowitsch, *My System*** (archive.org / Wikisource — *verify the specific edition's licence*) — the canonical positional concept catalogue, which is what D4 needs; ③ **Hambrick et al. (2014)** free full text on PubMed Central, to close D1's residual phrasing check; ④ **Gobet PDF already held** in `docs/pdf/`; ⑤ free descriptions of graded curricula (Steps Method syllabus, Lichess's own practice series) for *structure* rather than content. What we give up: modern graded curricula whose banding was F2's most attractive feature — revisit only if M2 is genuinely blocked without them. Original entry follows. — **Primary coaching sources unread.** [[domain.coaching]] rests on secondary web content, some of it commercial (risk R-11). Not defensible when an examiner asks on what basis the knowledge was decomposed — and that decomposition *is* the system architecture. | open | agent+author | M2 provenance, thesis defensibility | Target 4–6 sources that justify: (a) the concept decomposition → a published graded curriculum (Steps Method, Yusupov); (b) band-dependent priorities → a rating-banded course (Silman's endgame course); (c) the diagnosis taxonomy → Dvoretsky on training methodology; (d) learning-science claims → de Groot, Chase & Simon, Gobet, Hambrick. **Order: Tier 1 (peer-reviewed, free, retrievable) first** — strongest evidence and the material already cited without reading. For Tier 2 books, the published table of contents/syllabus structure is often enough for M2; only then decide what is worth buying. |

---

## Working order (proposed 2026-07-28)

1. **A1 + A2** — the engine benchmark. Unblocked, converts the largest unknown into a number, decides C5.
2. **F1** — read the five repos. Cheap, and must precede any M3 design.
3. **D2 + E1 + E3** — the mechanical verification batch; pairs naturally with A1.
4. **F2 Tier 1** — free primary sources; closes D1.
5. **B1–B3** — author's scope decisions, then M2.
